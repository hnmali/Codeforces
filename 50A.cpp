    #include <bits/stdc++.h>
    using namespace std;

    void solve()
    {
        int n, m;
        cin >> n >> m;
        cout << (m * n) / 2;
    }
    signed main()
    {
        ios::sync_with_stdio(0);
        cin.tie(0);
        cout.tie(0);
        long long int t;
        // cin >> t;
        t = 1;
        while (t--)
        {
            solve();
        }
    }
